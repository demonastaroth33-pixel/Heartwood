# PersonalOS

Private, single-user life-management app — journal + habits + goals + Coach.

**All documentation lives in [`docs/`](docs/README.md)** — read that first.
This root file exists only to route readers there.

- Docs entry point: `docs/README.md`
- Implementation: M0 not started yet (see `docs/Roadmap.md`)
